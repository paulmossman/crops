actions:
  print_env:
    command: env
    load_secrets: true
options:
  config:
    path: config.json
  secrets:
    path: secrets.json
  environment_file: ops.env
  environment:
    ENV_OPS_YML_VAR: "from ops.yml"
